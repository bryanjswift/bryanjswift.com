{
  // General configuration stuff goes in here
  "webapp": {
    // descriptor has to be on the classpath, WEB-INF gets copied to the classpath
    "descriptor": "WEB-INF/web.xml",
    "context": "/"
  },
  "http": {
    "port": 8080,
    "host": "localhost"
  },
  "templates": {
    "/index": {
      "html": "templates/index.vm"
    }
  }
}
